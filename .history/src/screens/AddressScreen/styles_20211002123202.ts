import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    root: {
        padding: 10,
    },
    row: {},
    label: {
        fontWeight: "bold"
    },
    input: {},
})

export default styles