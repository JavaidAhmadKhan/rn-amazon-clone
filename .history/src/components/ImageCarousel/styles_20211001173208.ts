import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    image: {
        margin: 10,
        height: 250,
        resizeMode: 'contain'
    },
    dot: {
        width: 25,
        height: 25,
        borderRadius: 25,
        borderWidth: 1,
        borderColor: '#c9c9c9'
    },
})

export default styles
