import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    root: {
        flexDirection: 'row',
    },
    button: {
        width: 25,
        height: 25,
    },
    buttonText: {},
    quantity: {},
})

export default styles