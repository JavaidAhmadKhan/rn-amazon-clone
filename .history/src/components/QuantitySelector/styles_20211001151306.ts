import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    root: {
        // flexDirection: 'row',
    },
    button: {},
    buttonText: {},
    quantity: {},
})

export default styles